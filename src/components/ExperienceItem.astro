<!-- ---
import BorderGlowButton from "../components/BorderGlowButton.astro"; // Asegúrate de que la ruta y la extensión sean correctas

export interface Props {
  title: string;
  description: string;
  link?: string;
  date: string;
}

const ExperienceItem = ({ title, description, link, date }: Props) => {
  ---
  return (
    <div>
      <div class="absolute size-3 bg-gray-200 rounded-full mt-1.5 -start-1.5 border border-white"></div>
      <time class="mb-1 mx-4 lg:mx-3 text-sm font-normal leading-none text-gray-400">
        {date}
      </time>
      <h3 class="text-lg font-semibold text-yellow-400">{title}</h3>
      <p class="mb-4 text-base font-normal text-gray-500 text-pretty">
        {description}
      </p>
      <BorderGlowButton />
      {link && (
        <a href={link} target="_blank" class="inline-flex items-center">
          Saber más...
          <svg
            xmlns="http://www.w3.org/2000/svg"
            class="h-4 w-4 ml-1"
            fill="none"
            viewBox="0 0 24 24"
            stroke="currentColor"
          >
            <path
              d="M17 8l4 4m0 0l-4 4m4-4H3"
            />
          </svg>
        </a>
      )}
    </div>
  );
};

export default ExperienceItem; -->
